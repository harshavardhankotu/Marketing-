# Autonomous Affiliate Marketing & ML Optimization Suite — Dependencies
# Install: pip install -r requirements.txt

Flask>=3.0
flask-login>=0.6.3
flask-wtf>=1.2
flask-limiter>=3.5
bcrypt>=4.0
python-dotenv>=1.0
requests>=2.31
APScheduler>=3.10
Pillow>=10.0

# Generative copywriter (Gemini 2.0 Flash)
google-genai>=1.0

# Video factory (optional — video falls back to poster rendering when absent)
moviepy>=2.0
gTTS>=2.5
numpy>=1.26

# Social distribution (Twitter/X)
tweepy>=4.14

# Testing
pytest>=8.0
